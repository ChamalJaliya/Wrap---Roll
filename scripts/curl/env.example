# Copy to scripts/curl/.env and adjust (do not commit .env if it holds secrets).
#   cp scripts/curl/env.example scripts/curl/.env

API_BASE=http://localhost:4000/api

# Supabase (password grant — same as seeded users in services/api seed)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=sb_publishable_your_anon_key

# Seeded test users (override as needed)
CUSTOMER_EMAIL=customer1@wrapnroll.com
CUSTOMER_PASSWORD=pass123
ADMIN_EMAIL=admin@wrapnroll.com
ADMIN_PASSWORD=pass123
CASHIER_EMAIL=cashier1@wrapnroll.com
CASHIER_PASSWORD=pass123

# PayHere (sandbox) — same as services/api/.env; hash + webhook tests need these
PAYHERE_MERCHANT_ID=1234981
PAYHERE_MERCHANT_SECRET="MzUyMzQ4MTM2MzI4ODMzMTAzNTUyOTYyMTcwNzM5MTc1ODM1OTg2NQ=="

# Optional: set after create-order so webhook script picks them up
ORDER_ID=
WEBHOOK_PAYMENT_ID=
